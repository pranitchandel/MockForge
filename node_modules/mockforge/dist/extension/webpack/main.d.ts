interface MockForgeOption {
    mockDataDir?: string;
    port?: number;
    host?: string;
}
export declare class MockForgeWebpackPlugin {
    private options;
    private port;
    private isMockEnabled;
    constructor(options?: MockForgeOption);
    apply(compiler: any): void;
}
export {};
