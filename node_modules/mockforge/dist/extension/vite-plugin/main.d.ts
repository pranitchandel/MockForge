interface MockForgeOption {
    mockDataDir?: string;
    port?: number;
    host?: string;
}
export declare function mockForge(options?: MockForgeOption): {
    name: string;
    configResolved(config: any): void;
    configureServer(): Promise<void>;
    transformIndexHtml(html: string): string;
};
export {};
