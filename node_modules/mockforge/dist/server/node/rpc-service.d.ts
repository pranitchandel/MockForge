import { WebSocket } from 'ws';
import { MockForgeStateService } from './service';
export declare class RPCClientManager {
    private mockForgeStateService;
    private rpcClient;
    constructor(mockForgeStateService: MockForgeStateService);
    onConnect(clientId: string, ws: WebSocket): void;
    private handleMessage;
    private broadcastEvent;
}
